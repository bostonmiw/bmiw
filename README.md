# Boston Medical Imaging Workshop website

Developed using `React.js`.

Check out the `gh-pages` branch and deploy website to GitHub pages using:

```console
$ npm run deploy -- -m "Deploy React app to GitHub Pages"
```